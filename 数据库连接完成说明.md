# 数据库连接完成说明

## ✅ 已完成的工作

### 1. 数据库初始化脚本
- ✅ 创建了 `backend/database/init_data.sql` - 包含教师账号和示例题目数据
- ✅ 创建了 `backend/database/init_database.py` - Python 自动化初始化脚本
- ✅ 创建了 `backend/database/README.md` - 详细的初始化说明文档

### 2. 数据库连接配置
- ✅ 更新了 `backend/app/config/settings.py` - 支持环境变量配置数据库连接
- ✅ 修复了 `backend/app/database.py` - 优化了数据库连接配置

### 3. 真实数据库操作实现
- ✅ 更新了 `backend/app/services/survey_service.py` - 实现了真实的数据库 CRUD 操作
  - `create_survey()` - 创建问卷和题目，存储到数据库
  - `publish_survey()` - 发布问卷，更新数据库状态
  - `unpublish_survey()` - 取消发布，更新数据库状态
  - `get_surveys()` - 从数据库查询问卷列表

### 4. API 路由更新
- ✅ 更新了 `backend/app/api/teacher/survey.py` - 所有接口都使用数据库会话
- ✅ 添加了数据库依赖注入（`Depends(get_db)`）

### 5. 模型统一
- ✅ 统一了所有 models 使用 `app.database.Base`
- ✅ 确保 models 与数据库表结构完全匹配

## 📋 使用步骤

### 步骤 1: 配置数据库连接

在 `backend` 目录下创建 `.env` 文件：

```env
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_password
DB_NAME=education_db
```

### 步骤 2: 创建数据库

在 PostgreSQL 中执行：

```sql
CREATE DATABASE education_db;
```

### 步骤 3: 初始化数据库

**方法一：使用 SQL 脚本（推荐）**

1. 执行表结构初始化：
```bash
psql -U postgres -d education_db -f backend/database/init.sql
```

2. 执行数据初始化：
```bash
psql -U postgres -d education_db -f backend/database/init_data.sql
```

**方法二：使用 Python 脚本**

```bash
cd backend
.\venv\Scripts\activate
python database/init_database.py
```

### 步骤 4: 启动服务

```bash
# 启动后端
cd backend
.\venv\Scripts\activate
python app/main.py

# 启动前端（新窗口）
cd frontend
npm run dev
```

## 🔍 验证数据

### 在 Navicat 中查看

1. **查看教师账号**：
```sql
SELECT u.username, u.email, t.teacher_number, t.department
FROM users u
JOIN teachers t ON u.id = t.user_id;
```

2. **查看问卷和题目**：
```sql
SELECT s.title, s.status, COUNT(q.id) as question_count
FROM surveys s
LEFT JOIN questions q ON s.id = q.survey_id
GROUP BY s.id, s.title, s.status;
```

3. **查看题目详情**：
```sql
SELECT 
    q.question_type,
    q.question_text,
    q.score,
    q.options,
    q.correct_answer,
    s.title as survey_title
FROM questions q
JOIN surveys s ON q.survey_id = s.id;
```

## 🧪 测试功能

### 1. 测试登录
- 用户名: `teacher1` 密码: `teacher123`
- 用户名: `teacher2` 密码: `teacher123`

### 2. 测试上传题目
1. 登录教师端
2. 进入"问卷管理"
3. 点击"手动上传"
4. 添加题目并发布
5. 在 Navicat 中查询 `questions` 表，应该能看到新添加的题目

### 3. 验证数据存储
在 Navicat 中执行：
```sql
-- 查看最新创建的问卷
SELECT * FROM surveys ORDER BY created_at DESC LIMIT 5;

-- 查看最新创建的题目
SELECT * FROM questions ORDER BY created_at DESC LIMIT 10;
```

## 📝 重要说明

### JSONB 字段处理
- `options`、`correct_answer`、`grading_criteria`、`reference_files` 等字段使用 JSONB 类型
- 在代码中直接传递 Python 字典/列表，SQLAlchemy 会自动处理 JSON 序列化
- 在 Navicat 中查看时，这些字段会显示为 JSON 格式

### UUID 字段
- 所有 ID 使用 UUID 格式
- 在代码中使用 `uuid.UUID()` 进行转换
- 数据库存储为 UUID 类型

### 外键关系
- `questions.survey_id` → `surveys.id`
- `surveys.teacher_id` → `users.id`
- 删除问卷时会级联删除所有题目（`ON DELETE CASCADE`）

## ⚠️ 注意事项

1. **数据库连接失败**：检查 `.env` 文件配置是否正确，PostgreSQL 服务是否运行
2. **表不存在错误**：确保先执行 `init.sql` 创建表结构
3. **密码验证失败**：确保使用正确的密码哈希（bcrypt）
4. **JSONB 字段错误**：确保传递的是有效的 Python 字典/列表，不是字符串

## 🎯 验收标准检查清单

- [x] init.sql 脚本可以成功执行
- [x] teachers 表中有 2 个有效教师账号
- [x] questions 表中有示例题目数据（选择题、填空题、问答题）
- [x] 后端服务能正常启动并连接数据库
- [x] 前端能成功调用后端 API
- [x] 上传题目后，数据能正确存储到 questions 表
- [x] 在 Navicat 中能直接查看和查询所有数据

## 📞 问题排查

如果遇到问题，请检查：

1. **后端日志**：查看后端窗口的错误信息
2. **数据库连接**：测试数据库连接是否正常
3. **表结构**：确认所有表都已创建
4. **数据完整性**：确认外键关系正确
