# 项目启动指南

## 快速启动

### 方法一：使用启动脚本（推荐）

1. **启动后端服务**
   - 双击运行 `start_backend.bat`
   - 或者右键选择"以管理员身份运行"

2. **启动前端服务**
   - 双击运行 `start_frontend.bat`
   - 或者右键选择"以管理员身份运行"

### 方法二：手动启动

#### 1. 启动后端服务

打开 PowerShell 或命令提示符，执行以下命令：

```powershell
# 进入后端目录
cd backend

# 激活虚拟环境
.\venv\Scripts\activate

# 启动服务
python app\main.py
```

后端服务将运行在：**http://localhost:8000**
- API 文档：http://localhost:8000/docs
- 健康检查：http://localhost:8000/health

#### 2. 启动前端服务

**新开一个** PowerShell 或命令提示符窗口，执行以下命令：

```powershell
# 进入前端目录
cd frontend

# 安装依赖（如果还没安装）
npm install

# 启动开发服务器
npm run dev
```

前端服务将运行在：**http://localhost:3000**

## 访问应用

启动成功后，在浏览器中访问：
- **前端应用**：http://localhost:3000
- **后端 API 文档**：http://localhost:8000/docs

## 注意事项

1. **需要先启动后端**，再启动前端
2. **保持两个窗口都打开**，关闭窗口会停止对应的服务
3. **数据库配置**：如果项目需要数据库，请确保 PostgreSQL 已安装并运行
4. **端口占用**：如果 8000 或 3000 端口被占用，请修改配置文件中的端口号

## 常见问题

### 后端启动失败
- 检查虚拟环境是否已激活
- 检查依赖是否已安装：`pip list`
- 查看错误信息，可能是数据库连接问题

### 前端启动失败
- 检查 Node.js 是否已安装：`node -v`
- 检查依赖是否已安装：查看 `node_modules` 文件夹是否存在
- 如果依赖未安装，运行：`npm install`

### 端口被占用
- 后端端口（8000）：修改 `backend/app/main.py` 中的端口号
- 前端端口（3000）：修改 `frontend/vite.config.ts` 中的端口号

## 停止服务

- 在对应的 PowerShell 窗口中按 `Ctrl + C`
- 或者直接关闭窗口
